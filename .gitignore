# === 系统与临时文件 ===
.quarto/
/.quarto/
/.claude/
*.stswp
.DS_Store
Thumbs.db

# === 素材：harvest 是从 ci-policy/stata101/ds2026 整包复制来的，不公开 ===
#     （体积大、且那三个仓库本身已公开，内容重复）
_materials/harvest/

# === images/：提交封面、logo、海报、二维码；其余（草稿版本、pptx）不入库 ===
images/*
!images/book-front.png
!images/book-logo.png
!images/poster-2026summer.png
!images/qrcode-2026summer.png

# 脏数据练习的"埋点答案清单"——仅老师本地留存，不发布（否则学员练习时会看到答案）。
working/01_data_messy_埋点答案.md

# demo_live/：上课现场演示用的一次性副本（demo 的镜像），不入库、不发布——
#   现场让 agent 跑动产生的中间文件不会污染发布的 demo/。demo/ 更新后重新
#   `cp -r demo demo_live` 同步即可；想保留某次结果，手动 git add -f。
demo_live/

# 说明：CLAUDE.md、working/、_task/、_materials/（harvest 除外）现在有意公开，
#       用于向学员展示"如何借助 AI Agent 制作本仓库"的完整过程。
# 注意：不要忽略 demo/，它是要随仓库发布的。
